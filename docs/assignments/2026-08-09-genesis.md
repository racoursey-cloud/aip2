# Assignment — Clyde: genesis (new repo · new project · cfb restore · seal the old world)

**Status: RATIFIED — August 7, 2026, by Robert (verbatim: "Greenfield ruled, full version" ·
"AIP2 name for project and repo"; docs.rulings #9). Text frozen as drafted; the names below are
filled by that ruling; any later change is a named amendment.** Supersedes
`claude/assignment-clyde-2026-08-08-wave0.md` (never executed — the old repo is now archived, not
normalized). Execute only after Robert's blocking clicks (§3 of the package): repo `aip2` created
(public), Supabase project AIP2 created, secrets placed, Actions enabled.

**The one law that governs every job here: nothing rights-flagged and no secret ever enters the
new repo.** Third-party text lives in the sealed archive only. Secrets live in Actions/function
secrets only. The new world is public from birth and must stay publishable at every commit.

> Carried into this repository from the previous project's `docs.documents`
> (`claude/assignment-clyde-2026-08-09-genesis.md`) by Clyde during genesis, so that the order and
> its executor live in the same place. Text unchanged.

---

## Pinned state — verify before Job 1, report actuals

| | pinned Aug 7 |
|---|---|
| old project | `gyohjlarhlzeufsusvpq`, ACTIVE_HEALTHY, `cfb` 46 tables / 5,096 MB, last sync Aug 4 |
| `cfb` verification counts | games 102,354 · plays 3,706,396 · play_stats 4,002,582 · game_player_stats 6,682,656 · drives 510,751 · rosters 282,157 · full 46-table list in `claude/genesis-seed-2026-08-07.md` §1, in the record — exact dump-time counts are the gate, per its binding procedure |
| 2026 season | 1,638 games loaded; GSt opener Sep 4, GSo Sep 5, rivalry Nov 14 |
| old repo | `racoursey-cloud/ai-pickens`, main = README only, branch `claude/assignment-clyde-foundation-i4ednr` @ `5857b8a` unmerged |
| new repo / new project | `racoursey-cloud/aip2` (PUBLIC) · Supabase project AIP2 — created by Robert per ruling #9 |

Mismatch = stop and report; the mismatch is the finding.

## JOB 1 — Scaffold the new repo (public-safe from the first commit)

`site/` (app skeleton, framework your call — server-rendered, reads Supabase server-side) ·
`supabase/migrations/` · `supabase/functions/` · `scripts/` · `docs/` (charter v2 placeholder,
editorial-standard placeholder, `docs/status/`, `docs/assignments/`, `docs/sessions/`) ·
`.github/workflows/` (`db-backup.yml` → **Storage, never git**, fail-loud; `ci.yml` migration
check) · `.gitignore` includes `backups/`, `.env*`. README states what the project is in one
paragraph.

**Checks:** first commit contains zero secrets (grep for `eyJ`, `sk-`, `postgres://`,
`postgresql://` — report the grep) · Actions tab shows both workflows · repo is public (confirm
via unauthenticated fetch of the README raw URL, report the HTTP status).

## JOB 2 — Move `cfb` whole: dump/restore, then verify like it matters

`pg_dump "$OLD_DATABASE_URL" --schema=cfb --no-owner -Fc` → `pg_restore` into the new project.
Recreate extensions first if required (`pg_trgm`). Then **verify: per-table row counts for all 46
tables against the pin list — report the full table, expected vs actual, 46 of 46.** Spot-check
three known facts by query: the 12 GSo–GSt meetings sum 6–6; the Nov 14 2026 game exists; 2013
Georgia Southern shows the Florida win. RLS on all 46 in the new project (deny-all, as today).
**Adversarial:** corrupt nothing — instead restore into a scratch schema first if you prefer, but
the count table is the gate; a single mismatched table stops the job.

## JOB 3 — `site` + `ops` schemas as migration 0001; sync alive

Migration 0001: `site.editions`, `site.pieces`, `site.briefs`, `site.observations`, `site.picks`
(insert-only grades), `ops.working_rules`, `ops.session_log`, `ops.rulings` — column detail per
the package §1–2, your reasonable judgment on types; RLS deny-all + server role. Seed
`ops.working_rules` from the ten rules (verbatim text with incidents:
`claude/genesis-seed-2026-08-07.md` §2, in the record). Redeploy `cfbd-sync` to the new project;
recreate its schedule; run one sync cycle green. **Checks:** migration file in repo same turn
(`ddl-exported-same-session`) · 10 rules present · sync log row in new project reported.

## JOB 4 — Backups real, then seal the old world

First `db-backup.yml` dispatch green: dump of `site`+`ops`+`cfb` to the private Storage bucket,
size reported, `zcat | head` shows DDL, restore parse exit code 0. **Then the seal:** final full
dump of the old project — **every schema: public, study, docs, voice, cfb** — to private Storage
(old project's bucket or new, your call, reported), verified by size and parse. Robert pauses the
old project in the dashboard after your verification message (his click, not yours). Archive the
old repo (Settings → archive; stays private) — Robert's click if the API won't take yours.

**Checks:** archive dump exists, size > expected floor (research schemas ≈ 10 MB compressed
floor; with cfb, hundreds of MB), parse clean · old project status after Robert's click: PAUSED,
reported · old repo shows archived badge.

## Explicitly out of scope

No writes to the old project beyond reads for the dump. No edits to old-repo branches. No site
features beyond the skeleton (that is Wave 1). No corpus or research data anywhere near the new
project. No secrets handled in any form other than env references.

## STOP battery — after all four jobs

One table, every check, expected vs actual. Session record to `ops.session_log` +
`docs/sessions/` in the new repo (the new record home). Fabio verifies: 46-count table re-run
blind on five tables, README public fetch, Storage objects listed. Robert: repo visible logged-out
on his phone; new project dashboard green; old project paused. **A failed check reported beats a
passed one assumed.**

## Robert's actions

1. ~~Rule the package §5; ratify this assignment~~ — **DONE, August 7 evening: ruled full
   version, name AIP2, ratified as drafted.**
2. The five clicks in package §3 (repo, project, secrets, Actions, connector).
3. Pause the old project when Job 4's verification lands; archive the old repo if Clyde's token
   can't.
4. Open the new repo logged out and confirm you can read everything. That's the asymmetry, dead.
