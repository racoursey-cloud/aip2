# Status notes

Where things stand, newest last. A status note supersedes the ones before it; any session
resumes from the most recent one.

Status notes are prose. The queryable half of the same picture lives in `ops.session_log` and
`ops.working_rules`.
