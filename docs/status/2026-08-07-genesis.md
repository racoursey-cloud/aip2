# Status — genesis, August 7 2026

**Supersedes every status note in the previous project's record.** Any future session resumes
from this one. The old world is described here only as something being sealed.

## Where things live now

| | |
|---|---|
| repo | `racoursey-cloud/aip2` — **public**, readable logged-out with no credentials |
| database | Supabase project **AIP2**, `kfgyzwxbxffhvckxmose`, PostgreSQL 17.6, us-west-2 |
| schemas | `cfb` (46 tables, carried whole) · `site` + `ops` (migration 0001) |
| the record | this repository. `docs/` for prose, `ops.session_log` and `ops.working_rules` for the queryable half |
| old project | `gyohjlarhlzeufsusvpq` — still ACTIVE, to be paused by Robert once the archive dump verifies |
| old repo | `racoursey-cloud/ai-pickens` — still private, to be archived |

## What is true as of tonight

- **cfb moved whole and was verified row for row**: 46 of 46 tables, 18,939,897 rows on both
  sides, three football spot-checks passing against the new copy. RLS deny-all on all 46.
- **The weekly sync is alive in AIP2.** Three pg_cron entries matching the old schedule, and one
  full cycle run by hand: 43 requests enqueued, `processed=43 errors=0 lost=0`, 44 log rows all
  `ok`, 6,571 rows affected.
- **The repository can rebuild the database.** `ci.yml` applies every migration to an empty
  PostgreSQL 17 on every push, and that check is green. This is the property the old world never
  had — 43 of 57 migrations there existed only in the database.
- **Nothing secret is in the repository**, and a gate proves it on every push rather than a
  human remembering to look.
- **The site is a skeleton that builds** — Next.js 15, server-rendered, reading Supabase with a
  key that cannot reach a browser bundle. It is not a site yet. That is Wave 1.

## Next actions, in order

1. **Robert reviews and merges** the genesis branch. Ratification is a PR approval now.
2. **Delete `genesis-transfer.yml` and `genesis-seal.yml`** after the merge. They exist only
   because `main` was empty during genesis; `db-backup.yml` is the permanent job.
3. **Robert's two clicks**, once the archive dump verifies: pause the old project, archive the
   old repo.
4. **A ruling on `cfbd-sync`** — it does not work and never did (session record, finding 3).
   Repair it or retire it.
5. **Wave 1** — the site, with the rivalry page, by ~Aug 15.

## The clock

Season opens **Aug 27**. Dress rehearsal on a Week 0 game, **Aug 27–29**. Live with North
Carolina A&T at Georgia State on **Sep 4** and Charleston Southern at Georgia Southern on
**Sep 5**. The capstone is **Nov 14**, Georgia State hosting Georgia Southern at Center Parc —
that game is in the database and was one of the three spot-checks.

Standing: October quarterly refresh of fund descriptions is a FundLens item, not this project's.
This project's standing item is the weekly loop, and it starts in three weeks.
