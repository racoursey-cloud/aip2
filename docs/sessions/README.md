# Session records

One file per working session: what changed, what was found, what is open, ranked.

Working rule **close-with-a-session-record**: every working session ends with one. Continuity is
the deliverable; reconstruction is where this project has repeatedly gone wrong.

Each record here has a matching row in `ops.session_log`.
